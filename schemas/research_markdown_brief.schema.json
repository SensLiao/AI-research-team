{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://research-os/schemas/research_markdown_brief.schema.json",
  "title": "Research Markdown Brief",
  "description": "Machine evidence that a human-readable deep_research Markdown brief was produced under director-review/research/.",
  "type": "object",
  "required": ["topic", "markdown", "evidence_refs", "perspective_ids"],
  "additionalProperties": false,
  "properties": {
    "topic": {"type": "string", "minLength": 1},
    "markdown": {
      "type": "string",
      "minLength": 1,
      "description": "The actual director-facing Markdown emitted by the deterministic renderer from validated structured evidence. Length, headings, and presentation coverage remain delivery advisories."
    },
    "evidence_refs": {"type": "array", "minItems": 1, "items": {"type": "string", "minLength": 1}},
    "perspective_ids": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "description": "Worker-declared navigation metadata. Actual perspective coverage is checked against the typed perspective notes and rendered with an advisory when incomplete."
    },
    "quality_caveats": {"type": "array", "items": {"type": "string"}, "default": []}
  }
}
