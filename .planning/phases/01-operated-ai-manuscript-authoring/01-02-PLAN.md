---
phase: 01-operated-ai-manuscript-authoring
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - schemas/manuscript_build_receipt.schema.json
  - schemas/manuscript_asset_manifest.schema.json
  - schemas/manuscript_quality_report.schema.json
  - schemas/manuscript_review_verdict.schema.json
  - tests/test_manuscript_delivery_schemas.py
autonomous: true
requirements:
  - OPER-02
  - LATX-02
  - ASST-01
  - AUDT-01
  - DELV-02
  - VERI-04
must_haves:
  truths:
    - "D-18: build state is receipt-derived and a compiled PDF cannot exist as a claim without source, process, log, and PDF hashes."
    - "D-19 and D-20: each figure/table is immutable-input, run-owned-output, label/caption/source/result/command bound."
    - "D-21 and D-22: daily usability and strict submission readiness are separate deterministic outputs."
    - "D-01: review verdicts bind a frozen manuscript and remain independent from generation evidence."
  artifacts:
    - path: schemas/manuscript_build_receipt.schema.json
      provides: "Truthful compiler state and receipt"
    - path: schemas/manuscript_asset_manifest.schema.json
      provides: "Figure/table provenance and ownership"
    - path: schemas/manuscript_quality_report.schema.json
      provides: "Findings, daily state, and submission state"
    - path: schemas/manuscript_review_verdict.schema.json
      provides: "Independent frozen-hash review verdict"
    - path: tests/test_manuscript_delivery_schemas.py
      provides: "RED/GREEN delivery schema tests"
  key_links:
    - from: schemas/manuscript_build_receipt.schema.json
      to: schemas/manuscript_quality_report.schema.json
      via: "source/PDF hashes and build finding"
      pattern: "pdf_sha256"
    - from: schemas/manuscript_review_verdict.schema.json
      to: schemas/manuscript_quality_report.schema.json
      via: "frozen manuscript hash and independent blocking findings"
      pattern: "manuscript_sha256"
---

<objective>
Define the closed build, asset, quality, and independent-review contracts that make delivery claims auditable under D-01 and D-18 through D-22.

Purpose: Prevent schema-valid-looking artifacts from laundering stale PDFs, unowned assets, reviewer contamination, or advisory defects into false truth/status claims.
Output: Four delivery schemas and truth-sensitive schema tests.
</objective>

<execution_context>
@C:/Users/廖神/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/廖神/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-operated-ai-manuscript-authoring/01-CONTEXT.md
@.planning/phases/01-operated-ai-manuscript-authoring/01-RESEARCH.md
@.planning/phases/01-operated-ai-manuscript-authoring/01-PATTERNS.md
@.planning/phases/01-operated-ai-manuscript-authoring/01-AI-SPEC.md
@schemas/run_manifest.schema.json
@schemas/panel_synthesis.schema.json
@schemas/venue_review.schema.json
@tests/test_validate_artifact.py
</context>

## Artifacts this phase produces

- schemas/manuscript_build_receipt.schema.json — manuscript_build_receipt.
- schemas/manuscript_asset_manifest.schema.json — manuscript_asset_manifest.
- schemas/manuscript_quality_report.schema.json — manuscript_quality_report.
- schemas/manuscript_review_verdict.schema.json — manuscript_review_verdict.
- tests/test_manuscript_delivery_schemas.py — direct delivery-contract tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive delivery and review schemas RED to GREEN</name>
  <read_first>
    - schemas/run_manifest.schema.json — receipt/state/source-of-truth conventions
    - schemas/panel_synthesis.schema.json — conditional verdict invariants
    - schemas/venue_review.schema.json — independent reviewer payload pattern
    - tests/test_validate_artifact.py — closed-schema negative cases
    - .planning/phases/01-operated-ai-manuscript-authoring/01-AI-SPEC.md — Sections 4.6, 4.7, 5, and 6
  </read_first>
  <files>schemas/manuscript_build_receipt.schema.json, schemas/manuscript_asset_manifest.schema.json, schemas/manuscript_quality_report.schema.json, schemas/manuscript_review_verdict.schema.json, tests/test_manuscript_delivery_schemas.py</files>
  <behavior>
    - COMPILED requires executable/version/argv/OS/return code/log/recorder/source/PDF hashes and a nonempty PDF fact; TOOLCHAIN_MISSING forbids a PDF claim; COMPILE_FAILED records observed failure.
    - Every asset has stable label, type, immutable source hash, run-owned output hash, caption owner, claim/result refs, reproducible render command or external provenance, permission, and accessibility text.
    - Quality reports classify hard versus advisory findings, derive one of four daily states, and carry a separate submission-ready boolean with blockers.
    - A requires_pdf build deficit may encode a readable non-BLOCK daily state together with submission_ready false and an explicit build blocker; schema conditions never force those axes to collapse.
    - Review verdicts require frozen contract/manuscript/PDF hashes, reviewer identity, blind-read receipt, scoped inputs, findings, and deterministic disposition.
  </behavior>
  <action>RED: write conditional-schema tests that demonstrate stale/fabricated PDF claims, asset overwrite ambiguity, advisory-to-BLOCK escalation, unbound review verdicts, and invalid daily/submission combinations are rejected; include a valid readable non-BLOCK plus submission_ready=false build-blocker case. GREEN: implement Draft 2020-12 closed schemas following schemas/run_manifest.schema.json receipt facts, schemas/panel_synthesis.schema.json allOf conditions, and venue-review schemas for blind identity. Per D-18, COMPILED is impossible without all observed build facts. Per D-19/D-20, asset ownership/provenance is complete. Per D-01/D-21/D-22, review evidence stays hash-bound, build blockers remain explicit, and daily readability versus submission status remains distinct.</action>
  <acceptance_criteria>
    - Each build state has mutually consistent required and forbidden facts.
    - Every visible asset is provenance-complete.
    - Review verdicts cannot omit blindness or frozen-input receipts.
    - Advisory findings alone cannot encode BLOCK, while hard truth findings cannot encode USABLE or submission-ready.
    - A PDF-required build deficit can remain readable while submission readiness is false, with separate rationales.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_manuscript_delivery_schemas.py -q</automated>
  </verify>
  <done>Delivery and independent-review payloads cannot represent internally inconsistent truth, ownership, or status states.</done>
</task>

</tasks>

<threat_model>
Coverage baseline: this plan keeps the vault/database read-only; rejects unsafe paths plus symlink/reparse escapes; redacts secrets from URLs and logs; treats TeX and subprocess use as deny-by-default unless the safe bounded adapter authorizes it; rejects false execution or PDF claims; and preserves worker blindness by validating authorization receipts against tampering.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| machine -> PhD-Research-OS | Vault material is untrusted read-by-reference input; this phase has no vault writer or promotion authority. |
| worker -> scheduler/run store | Worker bundles cross a least-privilege boundary and become visible only after schema, scope, dependency-receipt, and hash checks. |
| manuscript source -> filesystem/TeX process | Generated TeX and asset paths are untrusted until run-root, symlink/reparse, directive, and subprocess checks pass. |
| scholarly provider -> persisted artifacts | Provider URLs and errors may contain secrets and must be sanitized before ledger, build, or director-facing persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-VLT | Tampering / Elevation | vault and run paths | high | mitigate | Closed receipt and provenance schemas reject unowned paths, vault targets, missing input hashes, and unbound reviewer data; preserve bounded tools/recall.py reads, run-owned writes, unchanged-vault assertions, and no promotion imports. |
| T-02-SEC | Information Disclosure | scholarly URLs, errors, logs, TeX, packets | high | mitigate | Persist only sanitized provider/path/status/request facts and run sentinel-secret scans before any artifact becomes durable. |
| T-02-TEX | Elevation / Denial of Service | TeX input and compiler subprocess | high | mitigate | Fence paths including symlink/reparse targets, reject unsafe TeX directives, use fixed argv with shell disabled and bounded time/output. |
| T-02-TRU | Spoofing / Repudiation | result and PDF claims | high | mitigate | Accept execution only from frozen auditable result refs; derive compiled state only from a current source hash, process receipt, existing PDF, and PDF hash. |
| T-02-DAG | Tampering / Information Disclosure | worker inputs and reviewer blindness | high | mitigate | Require scheduler authorization receipts, declared predecessor slices, frozen hashes, and blind reviewer scopes before dependency visibility. |
| T-02-SC | Tampering | dependency supply chain | high | mitigate | Run no package-manager install; use only the repository Python standard-library/jsonschema/pytest/run-store/scheduler stack. |
</threat_model>

<verification>
Run python -m pytest tests/test_manuscript_delivery_schemas.py -q.
</verification>

<success_criteria>
- D-01 and D-18 through D-22 have machine-checkable payload contracts.
- False PDF, execution, asset ownership, and review independence states are structurally impossible.
- The schema family is ready for central registration in Plan 01-09.
</success_criteria>

<output>
Create .planning/phases/01-operated-ai-manuscript-authoring/01-02-SUMMARY.md when done.
</output>
