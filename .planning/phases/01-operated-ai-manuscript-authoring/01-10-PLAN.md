---
phase: 01-operated-ai-manuscript-authoring
plan: "10"
type: execute
wave: 2
depends_on:
  - "01-04"
  - "01-05"
  - "01-06"
files_modified:
  - orchestrator/roster.yaml
  - orchestrator/graph.yaml
  - orchestrator/mode_registry.yaml
  - tests/test_agent_connectivity.py
  - tests/test_capability_catalog.py
autonomous: true
requirements:
  - OPER-02
  - ORCH-01
  - ORCH-02
must_haves:
  truths:
    - "D-01: manuscript_authoring and manuscript_review are distinct routable contracts with separate evidence paths."
    - "D-02: both remain explicitly spec-only in this plan; neither enters the Python operated REGISTRY before a concrete recipe exists."
    - "D-13 through D-16: every new capability has a valid roster stage, graph edge, mode membership, and adaptive sparse-DAG role."
  artifacts:
    - path: orchestrator/roster.yaml
      provides: "Stage homes for seventeen manuscript roles"
    - path: orchestrator/graph.yaml
      provides: "Legal stage connectivity for manuscript roles"
    - path: orchestrator/mode_registry.yaml
      provides: "Honest spec-only authoring and review contracts"
    - path: tests/test_agent_connectivity.py
      provides: "Exact roster/graph/mode connectivity assertions"
    - path: tests/test_capability_catalog.py
      provides: "Spec-only maturity/count/target-product truth"
  key_links:
    - from: orchestrator/mode_registry.yaml
      to: orchestrator/graph.yaml
      via: "agent_subset stage compatibility"
      pattern: "manuscript_authoring"
    - from: tests/test_capability_catalog.py
      to: operate/modes/__init__.py
      via: "new modes are absent from REGISTRY and reported not push-button"
      pattern: "spec_only_not_push_button"
---

<objective>
Connect all manuscript roles and define two distinct honest spec-only mode contracts, implementing D-01/D-02 and D-13 through D-16 without claiming operation early.

Purpose: Give recipe development a valid router/scheduler contract while preserving the repository invariant that executable Python REGISTRY is the only operated truth.
Output: Roster, graph, declarative mode contracts, and connectivity/capability regressions.
</objective>

<execution_context>
@C:/Users/廖神/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/廖神/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-operated-ai-manuscript-authoring/01-04-SUMMARY.md
@.planning/phases/01-operated-ai-manuscript-authoring/01-05-SUMMARY.md
@.planning/phases/01-operated-ai-manuscript-authoring/01-06-SUMMARY.md
@orchestrator/roster.yaml
@orchestrator/graph.yaml
@orchestrator/mode_registry.yaml
@tests/test_agent_connectivity.py
@tests/test_capability_catalog.py
@operate/modes/__init__.py
</context>

## Artifacts this phase produces

- orchestrator/roster.yaml — seventeen new manuscript role entries.
- orchestrator/graph.yaml — stage allow-list edges for those roles.
- orchestrator/mode_registry.yaml — new manuscript_authoring contract and replacement manuscript_review contract, both spec-only.
- tests/test_agent_connectivity.py — updated exact counts and mode membership checks.
- tests/test_capability_catalog.py — updated spec-only maturity and target-product expectations.

<tasks>

<task type="auto">
  <name>Task 1: Add manuscript roles to the roster and stage graph</name>
  <read_first>
    - orchestrator/roster.yaml — discover/design/analyze/verify/report group structure
    - orchestrator/graph.yaml — allowed_agents and blocking_gates structure
    - tests/test_agent_connectivity.py::test_every_non_control_agent_has_graph_and_mode_entry — exact connectivity invariant
    - operate/panel_scheduler.py::validate_worker_spec_connectivity — stage/roster/mode validation
  </read_first>
  <files>orchestrator/roster.yaml, orchestrator/graph.yaml, tests/test_agent_connectivity.py</files>
  <action>Add the seventeen Plan 01-04/05/06 agent ids to one correct roster group and legal graph stage: reconnaissance in DISCOVER; architect/evidence steward in DESIGN; specialized and parameterized section authors plus figure/integrator roles in ANALYZE; factual, citation, venue/style-LaTeX, domain/contribution, methods/reproducibility, and figure/table reviewers in VERIFY; submission packager in REPORT. Follow existing group/allowed_agents structure. Update exact baseline-plus-delta assertions to roster_agents=157, non_control_agents=151, graph_connected_non_control=151, and mode_connected_non_control=151. Assert manuscript-section-author maps to ANALYZE authoring only, the three expert reviewers map to VERIFY review, and every manuscript id resolves to its intended stage/mode. Per D-14/D-16, author/reviewer labels remain distinct and no test encodes a fixed global worker or section-worker count.</action>
  <acceptance_criteria>
    - Every new non-control agent has exactly one roster group, at least one graph stage, and an eventual mode membership.
    - Existing agents and control counts change only by the seventeen deliberate additions: 157 roster agents and 151 non-control/graph-connected/mode-connected agents.
    - Graph validation stays clean.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_agent_connectivity.py tests/test_graph_spec.py -q</automated>
  </verify>
  <done>All manuscript roles are reachable only through their intended stages.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the old review-pack spec with distinct honest mode contracts</name>
  <read_first>
    - orchestrator/mode_registry.yaml::manuscript_review_pack — current registry_routable_spec_only product contract
    - orchestrator/mode_registry.yaml operated entries — stage_path, product contract, gate, and budget patterns
    - tools/capability_catalog.py::_mode_status and _validate_spec_only_product_maturity — derived honesty rules
    - tests/test_capability_catalog.py::test_spec_only_modes_are_honestly_not_push_button — expected catalog behavior
  </read_first>
  <files>orchestrator/mode_registry.yaml, tests/test_capability_catalog.py</files>
  <action>Replace manuscript_review_pack with distinct manuscript_review and manuscript_authoring contracts. Authoring uses DISCOVER, DESIGN, ANALYZE, VERIFY, REPORT; freezes required_sections; preserves specialized introduction/related-work/methods/results ownership; instantiates manuscript-section-author for every remaining required section; and requires exactly one candidate bundle per required section before integration. Add empirical, theory, dataset, survey, and system paper-type contract fixtures proving abstract, discussion/conclusion, limitations/ethics, appendix, and venue-required coverage without fixed section/worker counts. Review uses VERIFY/REPORT, consumes frozen contract/manuscript/source/PDF hashes, requires the exact domain_contribution, methods_reproducibility, figure_table, factual, citation, and venue_style_latex capability ids, at least two distinct blind reviewer authorization receipts, and a deterministic reconciliation/meta-review join. Both remain registry_routable_spec_only with required Markdown paths, reusable artifacts, explicit gaps, provider-neutral budgets, and operated false/absent. Keep the temporary catalog at sixteen spec-only total/six registry-routable and assert both are absent from Python REGISTRY per D-02.</action>
  <acceptance_criteria>
    - The old conflated manuscript_review_pack id no longer routes.
    - manuscript_authoring and manuscript_review are separate spec-only entries with different accepts/outputs/evidence.
    - Authoring contracts cover empirical, theory, dataset, survey, and system required sections exactly once without integrator-authored prose.
    - Review contracts require all six capability ids, two or more distinct blind receipts, and reconciled output without a fixed global panel count.
    - Capability catalog labels both not push-button and mirrors the unchanged Python REGISTRY.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_agent_connectivity.py tests/test_capability_catalog.py tests/test_graph_spec.py -q</automated>
  </verify>
  <done>Recipes can be built against two valid mode contracts without any false operated claim.</done>
</task>

</tasks>

<threat_model>
Coverage baseline: this plan keeps the vault/database read-only; rejects unsafe paths plus symlink/reparse escapes; redacts secrets from URLs and logs; treats TeX and subprocess use as deny-by-default unless the safe bounded adapter authorizes it; rejects false execution or PDF claims; and preserves worker blindness by validating authorization receipts against tampering.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declarative registry -> executable registry | YAML routing is not proof of an operated product. |
| roster/graph -> scheduler | Stage and mode membership constrain which worker can receive which data. |
| authoring -> review | Separate mode ids, runs, inputs, and products preserve independent evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-VLT | Tampering / Elevation | role scopes | high | mitigate | Mode contracts expose recall references only and contain no vault writer/promotion/GPU stage. |
| T-10-SEC | Information Disclosure | worker read scopes | high | mitigate | Graph/mode membership plus agent never-scopes prevent secret, sibling-review, and undeclared input exposure. |
| T-10-TEX | Elevation | author/build roles | high | mitigate | No worker receives shell authority; TeX execution remains a deterministic adapter capability. |
| T-10-TRU | Spoofing / Repudiation | operated status | high | mitigate | Both new YAML modes remain spec-only and catalog tests assert they are absent from Python REGISTRY. |
| T-10-DAG | Tampering / Information Disclosure | role connectivity | high | mitigate | Exact roster, graph, mode, and stage tests constrain author/reviewer separation and scheduler authorization. |
| T-10-SC | Tampering | dependency supply chain | high | mitigate | YAML/Markdown/test wiring adds no package or framework. |
</threat_model>

<verification>
Run python -m pytest tests/test_agent_connectivity.py tests/test_capability_catalog.py tests/test_graph_spec.py -q.
</verification>

<success_criteria>
- D-01, D-02, and D-13 through D-16 are reflected in routing and connectivity.
- The repository remains honest: routable does not mean operated.
- Both later recipe plans have stable stage and worker contracts.
</success_criteria>

<output>
Create .planning/phases/01-operated-ai-manuscript-authoring/01-10-SUMMARY.md when done.
</output>
