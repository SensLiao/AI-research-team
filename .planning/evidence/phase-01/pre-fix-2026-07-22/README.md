# Historical Pre-fix Evidence

These files record the first full-suite attempt on 2026-07-22. It correctly
reported two failures that were fixed before the release verdict:

1. three producer-declared manuscript artifact types lacked registered schemas;
2. a quality-scoreboard test still expected ten instead of twelve operated modes.

They are retained for traceability only and are **not** release evidence. The
authoritative passing full-suite report is the sibling
`../full-suite-junit.xml`, which has zero failures and errors.
